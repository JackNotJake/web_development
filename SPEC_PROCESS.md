# SPEC_PROCESS.md — 与 Superpowers 协作生成 Spec 与 Plan 的过程

> AI4SE 期末项目 · 足球赛事 Web 应用
> 本文档记录 brainstorming 关键节点、关键迭代、AI 建议的采纳/推翻,以及冷启动验证的客观证据。

---

## 一、brainstorming 关键节点与智能体追问

brainstorming 技能介入后,围绕"做什么、做多深、怎么验证"进行了多轮质询。以下是关键追问与我的处理:

### 节点 1:范围与深度("30 秒能说清为什么有人用吗?")
- **智能体追问**:足球预测类产品已很多(懂球帝、竞彩App),你这个的差异化是什么?能不能 30 秒说清?
- **我的修正**:原想法是"做个能看球+预测+讨论的网站",被追问后聚焦差异化——**可解释的预测模型**(Elo+Poisson 概率矩阵,而非黑盘口)+ **算法与社区投票加权融合**(冷启动有算法兜底)。这一差异化成了 SPEC §1 问题陈述的核心。

### 节点 2:预测算法深度("纯算法还是含社区?")
- **智能体追问**:预测是单轮确定性计算,还是含 agent 自主循环?(B.2 判定)
- **我的判定**:预测为单轮算法(Elo→λ→Poisson→融合),讨论为 CRUD,均**无自主多轮工具调用循环**,不构成 agent,不触发 harness 约束。这一判定写入 SPEC §8,避免了不必要的 harness 实现负担。

### 节点 3:工程深度落点("老师要深度优先,你的深度在哪?")
- **智能体追问**:通用要求 §3.4 说"深度优先",你的工程深度体现在哪?
- **我的回答**:深度落在三处——① 预测算法可解释可回测(Brier Score);② 凭据安全走 OS 钥匙串而非明文 env;③ 实时架构按 match 分 Room + JWT 握手。这三处写入 SPEC 验收标准。

---

## 二、关键迭代(3 轮以上)

### 迭代 1:数据源——真实 API 还是 Mock?
- **AI 建议**:用 Football-Data.org 免费层真实数据。
- **我的处理**:**采纳**。但发现免费层无赔率/统计、限速 10 次/分 → 设计了"服务端 cron 同步 + 缓存,前端绝不直连"的适配层(SPEC §5 架构、Task 8-9)。这与凭据安全要求协同:token 走 keytar。

### 迭代 2:部署——本地还是上云?
- **初版决策**:仅本地运行(图省事)。
- **AI 指出冲突**:通用要求 §4.11 + §五.9 明确"必须提供公网可访问 WebUI",仅本地不达标。
- **我的处理**:**推翻初版**,改为 Render/Railway 云部署,写入决策。这是"读清评分标准"的直接收益——差点因省事丢硬性分。

### 迭代 3(冷启动触发):预测融合维度——121 还是 11?
- **背景**:PLAN 初版 Task 4 产出 11×11(=121)矩阵,Task 5 融合却用 11 维数组,且写"展平**或**简化为三档"留了歧义。
- **冷启动 agent 暴露**:不同 agent 会得到不同模型,维度对不齐。
- **我的处理**:**统一为胜平负 3 档**(算法侧矩阵折叠、社区侧投票映射 3 档、融合在 3 档上),写入 SPEC §M3.1。详见第四节冷启动验证。

---

## 三、AI 建议的采纳与推翻

| AI 建议 | 采纳? | 理由 |
|---|---|---|
| 用 Football-Data.org 免费层 | ✅ 采纳 | 真实数据、免费、配合 keytar 演示凭据安全 |
| 预测用 Elo+Poisson | ✅ 采纳 | 可解释、可回测、有工程深度 |
| 仅本地运行 | ❌ 推翻 | 违反 §4.11/§五.9 公网 WebUI 硬性要求 |
| 融合用"矩阵展平或三档"(二选一) | ❌ 推翻(钉死三档) | 冷启动证明歧义会导致不同实现 |
| K 因子 20-40 区间 | ❌ 推翻(固定 32) | 区间无选择规则=死值,固定值可测可回测 |
| 结算"分差±1"(margin 解读) | ❌ 推翻(改为每队各±1) | margin 与每队±1 在反例 (2,0)vs(1,1) 发散,后者更直观 |
| 用 git worktree 隔离每功能 | ✅ 采纳 | 通用要求 §4.6/4.7 要求每 worktree 一 PR |
| 前端先 solo-design canvas 定稿再编码 | ✅ 采纳 | §3.6 强烈推荐 Open Design,且对齐设计稿降返工 |

---

## 四、冷启动验证(最关键的客观证据)

### 4.1 方法
按通用要求 §4.5:用**与主开发 agent 不同**的 agent(general_purpose_task subagent),在**新 session、不导入任何先前会话历史**的前提下,仅提供 `SPEC.md` + `PLAN.md`(Task 3 eloService、Task 4 poissonService 片段),令其自主实现并"遇到不确定即记录而非凭猜测继续"。

### 4.2 结果
- **实现产出**:eloService.js + poissonService.js,**8/8 测试全绿**(TDD 先红后绿完整执行)。
- **暴露的 spec/plan 缺陷**(按严重度):

| # | 缺陷 | 严重度 | 类型 |
|---|---|---|---|
| 9 | **Elo→λ 映射公式完全缺失**(SPEC/PLAN 均无) | 🔴 严重 | spec 空洞 |
| 10 | **融合维度不一致**(121 维 vs 11 维 vs 3 维) | 🔴 严重 | plan 内部矛盾 |
| 4 | poissonPmf λ=0 guard 错误(应 P(0)=1) | 🟠 中 | 代码边界 bug |
| 12 | 结算"分差±1"歧义(margin vs 每队±1) | 🟠 中 | spec 措辞 |
| 11 | w 动态下调函数未定义 | 🟠 中 | spec 空洞 |
| 1 | K 因子 20-40 区间无选择规则 | 🟡 低 | spec 歧义 |
| 13 | Dirichlet α 值未明确 | 🟡 低 | spec 未定值 |
| 5 | MAX_GOALS=10 截断无归一化规则 | 🟡 低 | spec 未规定 |
| 6 | 主客进球独立性假设未声明 | 🟡 低 | 局限未记录 |
| 2 | updateElo result 参数视角未文档化 | 🟡 低 | 隐式契约 |

### 4.3 据此对 SPEC/PLAN 的修订(diff 要点)

**修订 1(SPEC §M3.1 新增"预测算法精确契约")**:
- 新增 Elo→λ 映射公式:`expectedGoalDiff=(homeElo-awayElo)/eloDivisor`,`λ_home=(baseGoals/2+diff/2)×(1+homeAdv)`,钳制 [0.1,6.0]。
- 新增融合维度决定:**胜平负 3 档**(算法侧 `toThreeWay` 折叠、社区侧投票映射 3 档)。
- 新增 w 函数:`w=max(0.5, 0.7-0.02×min(votes,10))`。
- K 固定 32;Dirichlet α=1;λ=0 边界;高 λ 不归一化但返回 matrixSum。
- 结算改为"每队各±1"并给反例 (2,0)vs(1,1)。

**修订 2(代码)**:修复 `poissonPmf` λ=0 guard,P(0)=1、P(k>0)=0,补测试。

**修订 3(PLAN Task 5)**:测试从 4 条 11 维改为 8 条 3 档,签名 `computeLambdas` 加 `eloDivisor` 参数,新增 `toThreeWay`/`weightFunction`。

### 4.4 反思
冷启动验证**确实暴露了我和主 agent 共有的隐性上下文盲区**。最典型的是 Elo→λ 映射——我在 brainstorming 时"心里知道"大概怎么算,但从未写明,导致一个全新 agent 无法复现。这印证了通用要求 §4.5 的论断:"你和主 agent 在 brainstorming 会沉淀大量共享隐性上下文,这会让你严重高估 spec 的清晰度"。若不做冷启动,Task 5 实现会因维度歧义而各 agent 各做各的、且无法对齐测试。

---

## 五、brainstorming 技能的评价

### 做得好的地方
- 强制"先设计后编码"的硬门,阻止了直接动手。
- 要求"分块呈现设计供逐项签字",逼迫把模糊想法变成可签字的明确决策。
- 推动了"深度落点"的质询,使工程深度从口号变成具体的三处。

### 让我不满的地方
- 对"算法契约的精确度"督促不够——brainstorming 关心"做什么",但对"公式/参数定值"这类工程契约的精确性没有专门检查,是冷启动而非 brainstorming 本身暴露了这块。建议该技能增加"算法/数值契约是否全部定值"的 checklist。
- 设计稿与代码对齐的约束较弱(依赖 solo-design 兜底)。
