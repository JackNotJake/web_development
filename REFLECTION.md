# REFLECTION.md — 项目反思

> AI4SE 期末项目 · 足球赛事浏览·比分预测·赛后讨论
> 本文为学生本人撰写的反思报告,由 AI 辅助润色并标注。

---

## 一、项目概览与我的角色

本项目是一个整合“赛事浏览—比分预测—赛后讨论”的全栈 Web 应用。我同时承担了 PM、架构师、实现者与代码评审者的角色。项目使用 React + Vite + Tailwind 构建前端,Node.js + Express + Prisma + Socket.io 构建后端,预测模型采用可解释的 Elo + Poisson 算法,并与社区投票做加权融合。整个开发过程遵循 Superpowers 的七步工作流,并在实现前完成了 SPEC、PLAN 与冷启动验证。

回顾整个过程,我认为 Superpowers 的价值并不在于“让 AI 多写代码”,而在于它强制引入了一套在 AI 协作中极易被省略的工程纪律:TDD、规约先行、子任务拆分、人工评审。以下是我对这套方法论的几点具体反思。

---

## 二、哪些 Superpowers 技能发挥了最大作用

### 1. `brainstorming` —— 把模糊想法压成可签字的决策

在 brainstorming 阶段,智能体反复追问“这东西为什么有人用”“深度在哪”“是否构成 agent”。这些问题看似简单,却逼迫我从“做个足球网站”收敛到“用可解释模型做预测 + 社区投票融合”的差异化定位。没有这一步,后续实现会缺乏核心主线,很容易做成一个功能堆砌的 demo。

### 2. `writing-plans` —— 把 SPEC 拆成可验证的小步

PLAN.md 强制要求每个 task 2-5 分钟粒度、含文件路径、失败测试代码与验证命令。这对我自己(而非仅仅是 subagent)极其有用:它把“实现后端”这个模糊目标拆成了“先写 eloService 失败测试,再跑红,再实现,再跑绿”这样可执行的动作。在 AI 可以快速生成代码的环境下,这种粒度防止了“边想边写、写到哪算哪”的冲动。

### 3. `test-driven-development` —— 红绿循环是唯一的信任锚

TDD 在本项目中的作用被放大了。因为 AI 生成代码速度很快,如果没有“先写测试、看到失败、再写实现、看到通过”的约束,我很难判断一段代码是否真的正确,还是只是“看起来对”。例如 `poissonPmf` 在 λ=0 时的边界行为,正是通过失败测试才发现并修复的。TDD 不是阻碍,而是AI 协作下的放大器:它把 AI 的输出变成了可验证的假设。

---

## 三、哪些技能“形式大于实质”

### `subagent-driven-development` 的理想与现实

Superpowers 推荐每个 task 派一个新鲜 subagent,并在 task 间做两阶段评审。理论上这能最大化并行与客观性,但在我的实践中,subagent 的上下文切换成本很高:它需要完整的 SPEC、PLAN、文件路径、测试契约,而很多隐含约定(比如我们的错误响应格式 `{ error: string }`、Prisma 单例模式)无法在一次 prompt 中完全传递。结果 subagent 经常在“实现对了但没对齐项目风格”和“对齐了风格但漏了边界”之间反复。

因此,在时间紧迫的后期,我改为直接编码,但保留了 TDD 与验证纪律。这不是否定 subagent,而是认识到:**subagent 最适合边界清晰、上下文自包含的纯函数/独立模块(如 eloService、poissonService),而不太适合需要频繁跨文件协调的集成工作(如前端页面、Express app 组装)**。

### `using-git-worktrees` 的纪律成本

Superpowers 推荐每个独立功能开一个 worktree 并对应一个 PR。对于单人课程项目,我尝试在 mentally 划分了模块,但没有严格执行物理 worktree。原因很现实:频繁切换 worktree、保持依赖同步、合并冲突,对单人项目的 overhead 过高。我通过频繁的本地 commit 和小步迭代来弥补,但这确实削弱了“每个 worktree 一个 PR”的过程证据。

---

## 四、SPEC / PLAN 质量如何影响实现

最典型的案例是**预测融合维度**的歧义。

最初 PLAN 中写“将 11×11 矩阵展平或简化为三档进行融合”——“或”字是致命的。冷启动验证时,一个全新 agent 实现出的 `predictionService` 用了 121 维数组,而我的控制器期望的是 11 维或 3 维,导致接口对不上。这个 bug 不是实现问题,而是规约问题。后来我修订 SPEC §M3.1,把维度“钉死”为胜平负 3 档,并补全了 Elo→λ 映射公式、w 函数、λ=0 边界等全部定值。

这个案例让我意识到:**在 AI 协作中,spec 的清晰度会被你和主 agent 的共享上下文严重高估**。只有换一个完全陌生的 agent,才能暴露出“你心里知道但没写下”的假设。冷启动验证是本项目最有价值的环节之一。

---

## 五、最有效的 Prompt / Context 策略

### 1. 给 subagent 的 prompt 必须包含“失败测试 + 期望输出”

对冷启动 agent,我的 prompt 中强制要求:“先写失败测试,确认红后再写最少实现”。这比单纯说“按 SPEC 实现 eloService”有效得多,因为它把验收标准前置,agent 不容易偏离。

### 2. 用文件路径和函数签名锁定接口

在描述 task 时,我会明确写出:
- 创建文件:`server/src/services/eloService.js`
- 导出函数:`expectedScore(ra, rb)`、`updateElo(ra, rb, result, k=K)`
- 返回类型:`[number, number]`

这种精确性减少了 agent 的猜测空间。

### 3. 让 AI 暴露不确定性,而不是猜测

冷启动 prompt 中我加了:“遇到不确定之处必须暂停并记录,不要猜测”。这让 agent 在遇到 Elo→λ 映射缺失时不会胡编一个公式,而是明确列出问题,使我得以修订 spec。

---

## 六、凭据与分发迫使想清的问题

通用要求 §3.1 和 §3.2 是最容易被低估的两条,但它们实际上塑造了项目的很多决策:

1. **keytar 的使用**:我必须在开发机(Windows Credential Manager)和部署环境(Linux Secret Service 或平台 secret)之间做兼容。最终方案是“优先 keytar,失败回退 env”,并在 Dockerfile 中用 `--ignore-scripts` 避免 keytar 原生模块阻塞构建。
2. **token 绝不进 Git**:`.gitignore` 必须在项目第一天就配置好,而不是最后检查。
3. **Docker 分发**:为了让“别人能一键运行”,我不能只写“本地 npm run dev”。多阶段 Dockerfile、docker-compose、环境变量注入、数据库迁移自动执行,这些都是为了让目标机器上的安全配置变得可行。

这些工作不直接增加用户可见功能,却是工程可靠性的底线。

---

## 七、如果重做,我会改变什么

1. **更早做前端设计稿**:原计划用 solo-design / Open Design 产出 canvas 后再编码,但因上下文丢失和时间压缩被跳过。如果重来,我会在后端完成 50% 时就启动 solo-design,让视觉系统与数据结构并行推进,而不是最后直接手写 UI。
2. **更严格地执行 worktree/PR 工作流**:虽然单人项目 overhead 高,但课程明确要求“完整 commit/PR 历史”。我会至少把“预测模块”“讨论模块”“前端”“分发”四个大模块放到独立分支,保留更清晰的演进证据。
3. **增加前端组件测试**:目前测试集中在后端(64 tests),前端只有构建验证。如果时间允许,我会用 React Testing Library 覆盖登录、提交预测、发表评论等关键交互。

---

## 八、对 Superpowers 的批判

Superpowers 的隐含假设是:
1. 项目可以被提前完全规约;
2. subagent 在足够清晰的上下文下能可靠执行;
3. 严格的流程纪律能弥补单人工评审的不足。

在我的项目里,这些假设**部分成立**:

- **规约可以提前大部分确定**,但数值算法(如 Elo→λ、融合权重)的精确契约很难一次性写对,需要冷启动这种“外部反馈”来修正。
- **subagent 适合边界清晰的小任务**,但对需要频繁修改多个文件的集成任务,上下文传递仍然困难,人工干预不可避免。
- **流程纪律确实有用**,尤其是 TDD 和“先设计后编码”。但某些纪律(如物理 worktree 隔离)对单人项目的 overhead 可能超过收益,需要按项目规模灵活调整。

总体而言,Superpowers 是一套**“下限守护”**框架:它不能保证你做出伟大的产品,但能显著降低“没写测试就上线”“没想清楚就编码”这类低级错误。它的真正价值在于,让你在 AI 能写大量代码的时代,仍然保持对“什么是对的”的判断力。

---

## 九、结语

这个项目的核心训练目标不是代码量,而是“在 AI 能完成大部分编码工作时,工程师的真正价值在哪里”。我的答案是:**价值在于提出正确的问题、写出足够清晰的规约、设计可验证的测试、并在 AI 输出中识别和修正偏差**。Superpowers 是工具,而判断力才是最终交付物。
